import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  addItemContainer: {
    flexDirection: 'row',
  },
  addItemButton: {
    marginLeft: 10,
  },
});

export default styles;
